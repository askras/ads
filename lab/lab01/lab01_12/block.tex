\documentclass[tikz, margin=15mm]{standalone}
\usepackage[utf8]{inputenc}
\usepackage[russian]{babel}
\usetikzlibrary{shapes.geometric, arrows}

\tikzstyle{startstopblockstyle} = [
    rectangle, rounded corners=2.5mm, 
    minimum width=20mm, minimum height=10mm, 
    text centered, draw=black, fill=red!35
]
\tikzstyle{processblockstyle} = [
    rectangle,
    minimum width=40mm, minimum height=15mm,
    text centered, draw=black, fill=orange!20
]

\tikzstyle{processblockstyle} = [
    rectangle,
    minimum width=40mm, minimum height=15mm,
    text centered, draw=black, fill=orange!20
]

\tikzstyle{decisionblockstyle} = [
    diamond,
    minimum width=40mm, maximum height=10mm,
    text centered, draw=black, fill=green!20
]

\tikzstyle{arrow} = [
    thick, ->
]

\begin{document}
    \begin{tikzpicture} [node distance=40mm]
        \node (startnode) [startstopblockstyle] {Start};
        \node (processnode) [processblockstyle, below of=startnode, yshift=15mm] {minrun = 64};
        \node (process2node) [processblockstyle, below of=processnode, yshift=15mm] {inputarray};
        \node (process3node) [processblockstyle, below of=process2node, yshift=15mm] {arraysize};
        \node (process4node) [processblockstyle, below of=process3node, yshift=15mm] {if a=0};
        \node (decision1node) [decisionblockstyle, below of=process4node, yshift=15mm] {Sorted?};
        \node (shufflenode) [processblockstyle, left of=decision1node, yshift=-20mm] {return arraysize+a};
        \node (process5node) [processblockstyle, below of=shufflenode, yshift=15mm] {len of array};
        \node (process6node) [processblockstyle, below of=process5node, yshift=15mm] {left=0, right=len-1};
        \node (process7node) [processblockstyle, below of=process6node, yshift=15mm] {b=i, c=b-1, eachelement = inputarray[b]};
        \node (process8node) [processblockstyle, below of=process7node, yshift=15mm] {while eachelement<inputarray[c] and c>=left};
        \node (decision2node) [decisionblockstyle, right of=process8node, xshift=40mm] {Sorted?};
        \node (process9node) [processblockstyle, right of=decision2node, xshift=20mm] {inputarray[c+1] = inputarray[c], c -= 1};
        \node (process10node) [processblockstyle, right of=process9node, xshift=30mm] {inputarray[c+1] = eachelement};
        \node (process11node) [processblockstyle, below of=process10node, yshift=65mm] {return inputarray};
        \node (process12node) [processblockstyle, left of=process11node, xshift=-20mm] {mergedarraysize = minlengthrun};
        \node (shuffle2node) [processblockstyle, below of=decision2node, yshift=65mm] {return sorted array};
        \node (shuffle3node) [processblockstyle, right of=decision1node, yshift=-20mm] {return sorted array};
        \node (stopnode) [startstopblockstyle, below of=shuffle3node, yshift=15mm] {Stop};

        \draw [arrow] (startnode) -- (processnode);
        \draw [arrow] (processnode) -- (process2node)

    \end{tikzpicture}
\end{document}